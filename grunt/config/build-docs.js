module.exports = {
    options: {
        dest: '<%= paths.build %>',
        assetsDir: '<%= paths.docs %>/assets',
        pagesDir: '<%= paths.docs %>/pages',
        patternsDir: '<%= paths.docs %>/patterns',
        scssDir: '<%= paths.docs %>/scss',
        templatesDir: '<%= paths.docs %>/templates'
    }
};
